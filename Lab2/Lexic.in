Alphabet:

a. Upper (A-Z) and lower case letters (a-z) of the English alphabet

       b. Underline character '_';

       c. Decimal digits (0-9);

Lexic:

      a.Special symbols, representing:

- operators + - * / = < <= == >=

- separators ( ) [ ] { } , space newline ' "

- reserved words:

    and bool character constant do else float for if integer in or out read string sqrt then while write  

      b.identifiers

  -a sequence of letters and  digits, such that the first character is a letter; the rule is:

    identifier ::= letter | letter{letter}{digit}

    letter ::= "a" | "b" | ... | "z" | "A" | "B" | . ..| "Z"

    digit ::= "0" | "1" |...| "9"


      c.constants

1.integer

      noconst:="+"no|"-"no|no

      no:=digit{no}
	  
2.float

	  float:=no"."no
	  
	  no:=digit{no}

3.character

    character:='letter'|'digit'

4.string

      constchar:="string"

      string:=char{string}

      char:=letter|digit
	  
5.bool

	  bool:=0|1